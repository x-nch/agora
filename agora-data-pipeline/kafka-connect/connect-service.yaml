apiVersion: v1
kind: Service
metadata:
  name: kafka-connect
  namespace: city-services
  labels:
    app: kafka-connect
    component: data-pipeline
spec:
  selector:
    app: kafka-connect
  ports:
    - name: rest
      port: 8083
      targetPort: 8083
  type: ClusterIP
