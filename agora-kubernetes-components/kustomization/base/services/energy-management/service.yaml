apiVersion: v1
kind: Service
metadata:
  name: energy-management
  namespace: city-services
  labels:
    app.kubernetes.io/name: energy-management
spec:
  selector:
    app.kubernetes.io/name: energy-management
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
