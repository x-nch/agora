apiVersion: v1
kind: Service
metadata:
  name: data-broker
  namespace: city-services
  labels:
    app.kubernetes.io/name: data-broker
spec:
  selector:
    app.kubernetes.io/name: data-broker
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
